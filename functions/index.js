// Functions index - exports all available functions
const extractFiles = require('./extract-files');

module.exports = {
  extractFiles,
  // Add new functions here as they are created
  // convertFiles: require('./file-converter'),
  // generateReport: require('./report-generator'),
  // processData: require('./data-processor'),
};
